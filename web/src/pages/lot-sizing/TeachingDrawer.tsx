import type { SilverMealStep } from "../../lib/api";

// Placeholder — fully implemented with the step player in the next task.
export function TeachingDrawer(_props: { steps: SilverMealStep[] }) {
  return null;
}
